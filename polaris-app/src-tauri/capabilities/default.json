{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Polaris default capability set",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "fs:default",
    "shell:default",
    "shell:allow-execute",
    "shell:allow-spawn",
    "updater:default",
    "process:default"
  ]
}
