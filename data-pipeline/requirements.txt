# akshare 浮动取最新(数据源常修)；pandas/numpy 锁大版本上限，防 CI 构建拉到未验证的破坏性新版。
# 当前验证基准：akshare 1.18.64 / pandas 3.0.3 / numpy 2.4.5（与开发环境一致）。
akshare>=1.16.0
pandas>=2.2.0,<3.1.0
numpy>=1.26.0,<3.0.0

# 券商对接(可选,仅真实盘需要;Windows + 已登录券商客户端)。不装则账户页只用模拟盘 SimAdapter，
# 全流程照常可跑。需真实盘时:pip install easytrader
# easytrader>=0.23.0
